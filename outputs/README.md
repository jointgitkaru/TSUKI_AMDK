# Outputs

This directory is reserved for generated production material.

## Policy

- `audio/` = generated voice clips and sound work files
- `renders/` = generated keyframes, contact sheets, and review stills
- generated outputs are kept **local by default** and are not versioned automatically
- final masters can be exported later to `outputs/finals/` if needed
